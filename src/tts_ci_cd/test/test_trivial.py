def test_trivial():
	'''
	This is an extremely trivial test that's just here so the github action runs without failure.
	It should be replaced with real tests eventually, but these scripts are low risk, so we haven't
	gotten to it yet.
	'''
	assert True