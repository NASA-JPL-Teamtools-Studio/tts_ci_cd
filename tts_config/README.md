# TTS Config File Notes

This directory is not used directly, but is provided as an example of the config files needed to take advantage of all functionality in tts_ci_cd.

To use this directory, copy it to ~/.tts_config/ and update it with your details.

It is highly recommended to chmod this entire directory to 600 as there are keys that need to stay private. This locks reading and writing the file to only your user.